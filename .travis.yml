language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'sailor-sailor-module-base' ]; then cd .. && eval "mv $currentfolder sailor-sailor-module-base" && cd sailor-sailor-module-base; fi
